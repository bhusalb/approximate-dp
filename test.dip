NUMERIC TH = 0;
INPUT = [1, 0], [0, 1];
OUTPUT = [0, 0];
RANDOM RT = gauss(eps/4, TH);
RANDOM R1 = gauss(eps/4, INPUT[0]);
IF R1 >= RT THEN {
    OUTPUT[0] = 1;
} ELSE {
    RANDOM R2 = gauss(eps/4, INPUT[1]);
    IF R2 >= RT THEN {
        OUTPUT[1] = 1;
    }
}
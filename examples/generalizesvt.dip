INPUTSIZE 2;
RANDOM TH = gauss(eps/1, 0);
OUTPUT = [0, 0];
RANDOM Q1 = gauss(eps/2, INPUT[0]);
IF Q1 > TH THEN {
    OUTPUT[0] = 1;
} ELSE {
    RANDOM Q2 = gauss(eps/2, INPUT[1]);
    IF Q2 > TH THEN {
        OUTPUT[1] = 1;
    }
}
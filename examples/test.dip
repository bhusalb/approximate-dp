NUMERIC TH = 0;
OUTPUT = [0, 0];
RANDOM X = gauss(eps/4, TH);
RANDOM Y = gauss(eps/4, INPUT[0]);
RANDOM Z = gauss(eps/4, INPUT[1]);


IF X > Y THEN {
    OUTPUT[0] = 1;
    IF X > Z THEN {
        OUTPUT[1] = 1;
    }
}
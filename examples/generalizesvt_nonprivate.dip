INPUTSIZE 2;
RANDOM TH = gauss(eps/4, 0);
OUTPUT = [0, 0];
NUMERIC Q1 = INPUT[1];

IF Q1 > TH THEN {
    OUTPUT[0] = 0;
} ELSE {
    RANDOM Q2 = gauss(eps/2, INPUT[1]);
    IF Q2 > TH THEN {
        OUTPUT[1] = 1;
    }
}